
apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp
  namespace: apps
spec:
  replicas: 1
  selector:
    matchLabels:
      app: webapp
  template:
    metadata:
      labels:
        app: webapp
    spec:
      containers:
      - name: webapp
        image: acraksworkshopdev.azurecr.io/webapp:v1.0.3  # TODO: change
        ports:
        - containerPort: 80
